import { createDeployFunction } from "../utils/deploy";

const func = createDeployFunction({
  contractName: "DecreasePositionSwapUtils",

  id: "DecreasePositionSwapUtils_2",
});

export default func;
