import { createDeployFunction } from "../utils/deploy";

const func = createDeployFunction({
  contractName: "SwapPricingUtils",
  libraryNames: [],
  id: "SwapPricingUtils_2",
});

export default func;
